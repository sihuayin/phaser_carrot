# phaser_carrot

This project now uses Vite for local development and production builds while preserving the original Phaser gameplay code and asset paths.

## Scripts

- `pnpm install`
- `pnpm dev`
- `pnpm build`
- `pnpm preview`
